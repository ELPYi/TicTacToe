{
  "name": "tictactoe",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "cd client && npm install && cd ../server && npm install && cd ../client && npm run build",
    "start": "NODE_ENV=production node server/index.js"
  }
}
